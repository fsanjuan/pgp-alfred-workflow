{
  "name": "alfred-pgp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "reporters": [
      "default",
      ["jest-junit", { "outputDirectory": "reports", "outputName": "jest.xml" }]
    ]
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0"
  }
}
